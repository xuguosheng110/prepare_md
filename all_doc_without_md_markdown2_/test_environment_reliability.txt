
环境可靠性测试（定频）

测试方法


进入到test_tools/01_cpu_bpu_ddr文件夹下，执行sh cpubpu.sh &amp;脚本，对bpu，cpu进行压力测试，当前配置启动双bpu运行到最高99%负载，cpu 90%以上负载，内存随机读写
可以在脚本中更改运行时间及负载强度
打开终端记录运行log


测试标准


高温：45°、低温：-10°、常温下，程序正常执行，不会出现重启挂死的情况。
LOG中没有fail、error、timeout等异常打印。
能稳定运行48小时。
运行过程中需要关注CPU、BPU占比，使CPU占比均接近90%，BPU压力接近90%。